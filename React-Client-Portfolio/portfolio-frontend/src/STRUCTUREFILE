index.html 
    -> index.js 
        -> App.js 
            -> page_elements(HomePage, TestPage, etc.)
                -> components(HomePage=HeaderComponent, etc.) (sorted according to region in RealComponents folder)
                    -> code (HeaderComponent=Links, Logo, etc)
(core to abstracted)
(CSS files are linked to each component in region specific folder)
(Global COMPONENT CSS is App.css and is imported into App.js)
(Global LAYOUT CSS is index.css and is imported into index.css)